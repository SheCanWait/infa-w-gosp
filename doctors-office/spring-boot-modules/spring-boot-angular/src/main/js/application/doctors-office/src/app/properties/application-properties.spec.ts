import { ApplicationProperties } from './application-properties';

describe('ApplicationProperties', () => {
  it('should create an instance', () => {
    expect(new ApplicationProperties()).toBeTruthy();
  });
});
